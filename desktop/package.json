{
  "name": "manifest-network-desktop",
  "private": true,
  "version": "0.2.0",
  "description": "Manifest Network Desktop — private local AI with optional network participation",
  "scripts": {
    "tauri": "tauri",
    "icons": "tauri icon app-icon.svg",
    "build": "npm run icons && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
